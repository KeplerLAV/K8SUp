apiVersion: v1
kind: Service
metadata:
  name: {{ include "recrutement-app.fullname" . }}
  labels:
    {{- include "recrutement-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recrutement-app.selectorLabels" . | nindent 4 }}
