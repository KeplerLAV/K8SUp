apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "recrutement-app.fullname" . }}-test-connection"
  labels:
    {{- include "recrutement-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "recrutement-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
